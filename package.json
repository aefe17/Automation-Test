{
  "dependencies": {
    "@faker-js/faker": "^9.2.0",
    "cypress-mochawesome-reporter": "^3.8.2"
  },
  "devDependencies": {
    "@cypress-audit/lighthouse": "^1.4.2",
    "@cypress-audit/pa11y": "^1.4.2",
    "cypress": "^13.15.2",
    "cypress-audit": "^1.1.0"
  },
  "scripts": {
    "test": "npx cypress run",
    "report": "npx cypress run --reporter cypress-mochawesome-reporter"
  }
}
